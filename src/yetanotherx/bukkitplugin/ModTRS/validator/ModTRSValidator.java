package yetanotherx.bukkitplugin.ModTRS.validator;

/**
 * Plain interface, so the types can be based around a single type
 */
public interface ModTRSValidator {

    public boolean isValid( String[] args );

}
